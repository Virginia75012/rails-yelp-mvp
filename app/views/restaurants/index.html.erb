<div class="container mt-5">
  <div class="row">
    <div class="col">
      <h1 class="text-center">All Restaurants</h1>
      <ul class="list-group list-group-flush mb-5">
        <% @restaurants.each do |restaurant| %>
          <li class="list-group-item"><%= restaurant.name %><%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary d-block float-right" %></li>
        <% end %>
      </ul>
      <%= link_to "Add a new Restaurant", new_restaurant_path, class: "btn btn-primary" %></p>
    </div>
  </div>
</div>
